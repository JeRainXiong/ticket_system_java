<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE configuration  
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"  
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
<settings>
	<setting name="mapUnderscoreToCamelCase" value = "true"/>


</settings>
  <!-- 如果是纯粹的使用MyBatis，通常会配置如下节点。 -->
  <!-- <properties>数据库的属性文件</properties>
  <typeAliases>实体类的别名</typeAliases>
  <settings>MyBatis框架自身的属性设置</settings>
  <environments default=""> 
         开发，测试、运行时不同的环境配置
  </environments>-->
  <!-- 如果与Spring框架整合，则这个配置文件中除setting 节点的配置，其它的都可以移动到Spring的配置文件中进行配置。-->
  <typeAliases>
    <!-- 把这个包中的类都取一个别名，便于操作，因为类的使用时，需要这个类的全限定名。
       别名的命名规则是：类名=别名   com.icss.entity.Student==Student
    -->
    <package name="com.whut.entity"/>
  </typeAliases>
</configuration>